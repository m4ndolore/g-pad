# Copy this to oracle.env and fill in your key to use the HTTP oracle backend.
#   cp oracle.env.example oracle.env
# oracle.env lives NEXT TO THE BINARY — on the device that is the app folder,
# e.g. /home/root/xovi/exthome/appload/g-pad/oracle.env.
#
# Any OpenAI-compatible, vision-capable endpoint works.

RIDDLE_OPENAI_KEY=sk-your-key-here

# Optional — defaults shown:
# RIDDLE_OPENAI_BASE=https://api.openai.com/v1
# RIDDLE_OPENAI_MODEL=gpt-4o-mini
# Optional per-gesture model: a plain underline uses RIDDLE_OPENAI_MODEL;
# finishing the underline as a right arrow uses this model instead.
# RIDDLE_OPENAI_ASK_MODEL=vellum-ask

# Vellum example (the key is VELLUM_DEVICE_TOKEN, not a provider key):
# RIDDLE_OPENAI_BASE=https://vellum.example.com/v1
# RIDDLE_OPENAI_KEY=your-vellum-device-token
# RIDDLE_OPENAI_MODEL=vellum-capture
# RIDDLE_OPENAI_ASK_MODEL=vellum-ask
# RIDDLE_LEARN_MODEL=vellum-tutor
# RIDDLE_IDLE_MS=0

# Learn mode's tutor must never talk to a capture sink (it would archive the
# worksheet instead of marking it), so its asks use RIDDLE_LEARN_MODEL, else
# RIDDLE_OPENAI_ASK_MODEL, else the base model. On Vellum, vellum-tutor is
# the straight vision chat built for this. A correct answer deals the next
# page by itself after RIDDLE_LEARN_NEXT_MS (default 5000; 0 turns it off).
# RIDDLE_LEARN_MODEL=vellum-tutor
# RIDDLE_LEARN_NEXT_MS=5000

# Runaway guard on the reply. Thinking models (Gemini 3.x, o-series…) count
# their hidden reasoning tokens against this, so keep it roomy — a tight cap
# cuts the visible reply down to a single sentence.
# RIDDLE_OPENAI_MAX_TOKENS=2000

# For reasoning models only: sent as "reasoning_effort" when set. "low" makes
# the ink start sooner. Leave unset for non-reasoning models — some providers
# reject the field.
# RIDDLE_OPENAI_REASONING=low

# Example: OpenRouter
# RIDDLE_OPENAI_BASE=https://openrouter.ai/api/v1
# RIDDLE_OPENAI_MODEL=openai/gpt-4o-mini

# Example: Gemini (OpenAI-compatible endpoint)
# RIDDLE_OPENAI_BASE=https://generativelanguage.googleapis.com/v1beta/openai
# RIDDLE_OPENAI_MODEL=gemini-3.5-flash
# RIDDLE_OPENAI_REASONING=low

# --- pi backend (used only when RIDDLE_OPENAI_KEY is not set) ---
# Where pi lives and what it runs. Defaults shown. Note: pi keeps its own
# session history under its data dir; the HTTP backend keeps nothing.
# RIDDLE_PI_BIN_DIR=/home/root/node/bin
# RIDDLE_PI_PROVIDER=openai-codex
# RIDDLE_PI_MODEL=gpt-5.4-mini

# --- the pad's memory ---
# The pad remembers finished pages (your pen strokes, a transcription, and
# the reply) in /home/root/g-pad-data/memories, entirely on the tablet.
# Recent pages ride along with each request so the pad can follow a conversation,
# and writing "show me…" restores a past page back in your own hand.
# RIDDLE_MEMORY=off          # forget nothing more, remember nothing new
# RIDDLE_MEMORY_DIR=/home/root/g-pad-data/memories
# RIDDLE_MEMORY_TURNS=6      # how many recent pages ride into a reply
# RIDDLE_TZ_OFFSET=-4        # hours from UTC, so memory dates read right

# --- debugging ---
# Keep the committed page at /tmp/g-pad-page.png after each turn instead of
# deleting it once the oracle has read it. Handy with `g-pad --oracle-test`.
# RIDDLE_KEEP_PAGE=1

# --- feel tuning (windowed/qtfb mode) — defaults shown ---
# RIDDLE_FLUSH_MS=12    # ink update batching; lower = snappier, more CPU
# RIDDLE_IDLE_MS=0      # default: underline is the only send trigger
# Set a positive compatibility value (for example 2800) to opt into idle-send.
# Once saved on-device, the local SETTINGS preference takes precedence.
# RIDDLE_REPLY_WIDTH=4  # reply pen thickness; thicker reads darker on fast waveforms

# The reply hand: any TTF/OTF dropped next to the binary (or an absolute
# path). Unset = the embedded Dancing Script.
# RIDDLE_FONT_FILE=Lora.ttf

# The voice: put a system prompt in persona.txt next to the binary (or point
# RIDDLE_PERSONA_FILE at one) to replace the default pad voice.
# RIDDLE_PERSONA_FILE=persona.txt

# Reply draw speed: points drawn per animation frame. Higher = the answer
# appears faster. Default 120.
# RIDDLE_REPLY_POINTS=120

# Track B (write in the stock notes app, then open g-pad): ask the oracle
# about the newest rendered xochitl page instead of a blank pad. Set to 1 to
# enable. RIDDLE_ASK_MAX_AGE (seconds, default 900) ignores a page you didn't
# just write. RIDDLE_XOCHITL_DIR overrides the notebook store path.
# RIDDLE_ASK_XOCHITL=1
